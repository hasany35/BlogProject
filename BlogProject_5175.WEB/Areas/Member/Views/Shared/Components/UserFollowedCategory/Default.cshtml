@model List<Category>

<div class="text-center">
    <div class="col-md-12">
    <h3>takip edilen kategoriler</h3>
    </div>
    <p> qweweqweqwe </p>
    @if (Model.Count>0)
   {
       @foreach (var item in Model)
       {
           <button type="button" class="btn btn-block btn-outline-info" asp-action="Filter" asp-controller="" asp-route-id="@item.ID">@item.Name</button>
            
        }

    }
    else
    {
        <p>Takip edilen </p> 
            <br><br />
           <p> kategori yok</p>
        <br><br />
            <p> inceleyin => 
                <br><br />
                <a asp-action="List" asp-controller="Category" >tiklayin</a></p>
    }
</div>